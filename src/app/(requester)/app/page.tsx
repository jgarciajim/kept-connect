// Requester app home → "/app"
export default function RequesterHome() {
  return (
    <main className="mx-auto flex max-w-3xl flex-col gap-4 px-6 py-16">
      <h1 className="text-2xl font-semibold">Requester app</h1>
      <p className="text-foreground/70">
        Where homeowners, property managers, and realtors create and track requests.
      </p>
      <p className="text-sm text-foreground/50">Requester surface — placeholder.</p>
    </main>
  );
}
